services:
  sd-api:
    build: .
    container_name: sd-api
    ports:
      - "8000:8000"
    environment:
      - SD_MODEL=runwayml/stable-diffusion-v1-5
    # GPU inside container (optional)
    # deploy:
    #   resources:
    #     reservations:
    #       devices:
    #         - capabilities: ["gpu"]
